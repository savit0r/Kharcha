# Server
PORT=3000
NODE_ENV=development

# Database (Neon PostgreSQL or any PostgreSQL)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# JWT Secrets (generate strong random strings)
JWT_SECRET=your_jwt_secret_here
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here

# SMTP (for OTP emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your@email.com
SMTP_PASS=your_smtp_password

# Cloudinary (for image uploads - optional)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Redis (optional - for background job queues)
REDIS_URL=redis://localhost:6379

# Frontend URL (for CORS) — production example: https://kharcha-mauve.vercel.app
CLIENT_URL=http://localhost:5173

# Extra allowed browser origins (comma-separated), e.g. Vercel preview deploys
# ALLOWED_ORIGINS=https://kharcha-mauve-git-main-xxx.vercel.app
